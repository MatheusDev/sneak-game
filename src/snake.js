class snake {

  constructor(x, y, width, height) {
    this.x = x
    this.y = y
    this.width = width
    this.height = height
  }

  handleRun = (value) => {
    return setTimeout()
  }
}

export default snake